# dependencies
node_modules

# build output (all workspace packages/apps build from source)
packages/*/dist
apps/*/dist
examples/*/dist
.astro
*.tsbuildinfo

# test + tooling artifacts
test-results
playwright-report
examples/demo/screenshots

# misc
*.log
.DS_Store
Archive.zip
*.tgz

# The reference SKIN used by the DOM demo is the game studio's own stylesheet +
# icon font — wired up locally, never committed.
examples/demo/public/skin/
